//Implement the CUDA kernel :
//
//Each thread computes one pixel.
//Iterate the Mandelbrot function until the max iterations or escape condition.
//Store the iteration count for coloring.
//
//Implement a host function to launch the kernel and copy the results back to the host.